# CLAUDE.md

This file is a thin pointer to the repository's single source of truth.

Read **`AGENTS.md`** first before doing anything here.

Installed skills live in `.claude/skills/`, `.agents/skills/` and
`.opencode/skill/`; locked versions are in `skills-lock.json`.
